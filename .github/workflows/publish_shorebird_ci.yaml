name: Publish sankofa_ci

on:
  push:
    tags:
      - "sankofa_ci-v[0-9]+.[0-9]+.[0-9]+*"

jobs:
  publish:
    uses: ./.github/workflows/publish_dart_package.yaml
    with:
      working_directory: packages/sankofa_ci
      tag_prefix: sankofa_ci-v
    permissions:
      id-token: write
